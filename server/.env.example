# Environment variables for Porte Hobe AI Server

# Tavily Search API Key (required for search functionality)
TAVILY_API_KEY=your_tavily_api_key_here

# Groq API Key (optional - for validating solutions against large models like llama-70B)
# Get your key from https://console.groq.com/
# GROQ_API_KEY=your_groq_api_key_here

# Supabase configuration (required for auth + database)
# Get these from Supabase > Project Settings > API
SUPABASE_URL=https://your-project-ref.supabase.co
# Use the service_role key on the backend only (never expose to frontend)
SUPABASE_KEY=your_service_role_or_anon_key
# JWT secret shown under API settings; used to verify user JWTs
SUPABASE_JWT_SECRET=your_jwt_secret

# Optional: where frontend calls this backend from
FASTAPI_URL=http://localhost:8000

# Optional: Custom model name for Ollama (default: qwen2.5:3b-instruct-q5_K_M)
# OLLAMA_MODEL=qwen2.5:3b-instruct-q5_K_M

# Optional: Ollama host (default: localhost:11434)
# OLLAMA_HOST=localhost:11434
