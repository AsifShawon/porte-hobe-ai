import { PlaceholderPage } from "@/components/PlaceholderPage";
import { FileText } from "lucide-react";

export default function ResourcesPage() {
  return (
    <PlaceholderPage
      title="Resources"
      description="Your saved materials, notes, and bookmarks"
      icon={FileText}
    />
  );
}
